[project]
name = "git-visualiser-api"
version = "0.1.0"
description = "FastAPI backend for Git Visualiser."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.34.0",
  "pydantic-settings>=2.8.0",
  "python-dotenv>=1.0.1",
  "httpx>=0.28.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
